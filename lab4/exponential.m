function [f] = exponential(t,N,a)
%exponetial:
%   The funtion calculates an exponential with parameters t, N, and a.
    %The inputs are t which is time, 
    %N which is the intial population,
    % and 'a' which is another parameter.
        %Eduardo Carrasco Jr. 9/15/14
f=a*N;
end
